# eslint-config-next@14.2.x peer-depends on eslint ^7||^8, but the project uses
# eslint 9 (flat config). The conflict is dev/lint-only (not runtime), so allow
# npm to install past it on clean installs (CI/Vercel), matching local.
legacy-peer-deps=true
